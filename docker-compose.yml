services:
  weather-app:
    build: .
    container_name: weather-app

    ports:
      - "5000:5001"

    env_file:
      - .env

    volumes:
      - ./config.json:/app/config.json

    restart: unless-stopped
